<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MindfulMoments" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowSplashScreenBackground">@color/black</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="ToolbarBlackText" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@android:color/black</item> <!-- Color of back button and other control items -->
    </style>
    
    <!-- res/values/styles.xml -->
    <style name="GrayButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/gray_button_background</item>
        <item name="android:textColor">@color/black</item>
        <!-- Add other button style attributes here if needed -->
    </style>


    <style name="Theme.MindfulMoments" parent="Base.Theme.MindfulMoments" />
</resources>
